{
  "name": "rspack-minimizer-nondeterminism-test",
  "version": "1.0.0",
  "private": true,
  "description": "Reproduction case: non-deterministic CSS minimizer + Rspack [fullhash]",
  "scripts": {
    "build": "rspack build"
  },
  "devDependencies": {
    "@rspack/cli": "^2.0.1",
    "@rspack/core": "^2.0.1",
    "css-loader": "^7.1.4"
  }
}
