{
  "name": "rspack-fullhash-bug-test",
  "version": "1.0.0",
  "private": true,
  "description": "Reproduction case for Rspack fullhash bug with CSS-only changes",
  "scripts": {
    "build": "rspack build"
  },
  "devDependencies": {
    "@rspack/cli": "^2.0.1",
    "@rspack/core": "^2.0.1",
    "css-loader": "^7.1.4"
  }
}
